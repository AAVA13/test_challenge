// Importa el paquete necesario
import 'package:flutter/material.dart';

// Define una clase para los colores de la aplicación
class AppColors {
  // Define el color de fondo
  static const backgroundColor = Color(0xFFf3f7fe);
  // Define el color primario
  static const primaryColor = Color(0xFF224299);
  // Define el color secundario
  static const secondaryColor = Color(0xFFf8c410);
  // Define el color de advertencia
  static const warning = Color(0xFFD03047);
}

// Define una clase para los márgenes de la aplicación
class AppMargins {
  // Define el margen de separación
  static const separation = EdgeInsets.only(top: 10, left: 10);
}
